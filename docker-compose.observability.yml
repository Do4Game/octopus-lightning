services:
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: jaeger-otel
    ports:
      - "16686:16686" # UI webowe
      - "4318:4318"   # OTLP HTTP receiver
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    restart: unless-stopped
